---
phase: 02-validation-batch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/audio/elevenlabs-tts/SKILL.md
  - skills/video/google-veo/SKILL.md
autonomous: true
requirements:
  - AUD-01
  - VID-02
  - QUAL-03

must_haves:
  truths:
    - "Skill elevenlabs-tts tem SKILL.md com curl para api.elevenlabs.io/v1/text-to-speech/{voice_id} usando -f e --output para saída binária"
    - "Skill elevenlabs-tts verifica tamanho do arquivo de saída após download ([ -s file ])"
    - "Skill google-veo tem SKILL.md que detecta se gcloud está disponível e orienta o usuário sobre requisito GOOGLE_CLOUD_PROJECT"
    - "Skill google-veo implementa polling assíncrono (Long-Running Operation) com max_attempts configurável"
    - "Skill com chave ausente exibe mensagem human-readable orientando para /pocket-knife:setup"
    - "Ambos os skills têm disable-model-invocation: true conforme QUAL-01 (side effects: cobrança, arquivo binário)"
  artifacts:
    - path: "skills/audio/elevenlabs-tts/SKILL.md"
      provides: "Text-to-speech via ElevenLabs — saída binária MP3"
      contains: "api.elevenlabs.io/v1/text-to-speech"
    - path: "skills/video/google-veo/SKILL.md"
      provides: "Geração de vídeo via Google Veo (Vertex AI)"
      contains: "aiplatform.googleapis.com"
  key_links:
    - from: "skills/audio/elevenlabs-tts/SKILL.md"
      to: "https://api.elevenlabs.io/v1/text-to-speech/{voice_id}"
      via: "curl -f --output para saída binária MP3"
      pattern: "api.elevenlabs.io/v1/text-to-speech"
    - from: "skills/video/google-veo/SKILL.md"
      to: "https://us-central1-aiplatform.googleapis.com/v1/projects/{PROJECT_ID}/..."
      via: "gcloud auth print-access-token + polling loop"
      pattern: "aiplatform.googleapis.com.*veo"
---

<objective>
Criar dois SKILL.md de validação — elevenlabs-tts (AUD-01) e google-veo (VID-02) — que provam o round-trip para os padrões mais complexos: saída binária (audio MP3) e polling assíncrono (Vertex AI Long-Running Operation).

Purpose: Validar o padrão de saída binária com verificação de integridade ([ -s file ]) para ElevenLabs, e o padrão de job assíncrono com polling para Google Veo via Vertex AI. Esses dois padrões são os templates para os skills de áudio e vídeo da Phase 4.

Output: Dois SKILL.md funcionais — um com saída binária MP3 e um com polling async de vídeo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-validation-batch/02-RESEARCH.md

<interfaces>
<!-- Padrões extraídos do RESEARCH.md para skills com output binário e async -->

Padrão para saída binária (ElevenLabs — AUD-01):
```bash
# USAR -f (não --fail-with-body) para binário — evita misturar body de erro com stream binário
OUTPUT_FILE="$HOME/Downloads/tts_$(date +%Y%m%d_%H%M%S).mp3"
curl -f -s \
  -X POST "https://api.elevenlabs.io/v1/text-to-speech/${VOICE_ID}" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"$TEXT\", \"model_id\": \"eleven_multilingual_v2\"}" \
  --output "$OUTPUT_FILE"

if [ $? -ne 0 ]; then
  echo "ERROR: ElevenLabs API call failed. Verify ELEVENLABS_API_KEY."
  exit 1
fi

# OBRIGATÓRIO: verificar se arquivo tem conteúdo (Pitfall 3 do RESEARCH.md)
if [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Audio file is empty. Check the input text and your API key."
  exit 1
fi
```

Vozes ElevenLabs estáveis (IDs permanentes do RESEARCH.md):
- George: JBFqnCBsd6RMkjVDRZzb (Britânico, Masculino)
- Rachel: 21m00Tcm4TlvDq8ikWAM (Americano, Feminino)
- Aria: 9BWtsMINqrJLrRacOk9x (Americano, Feminino)
- Charlie: IKne3meq5aSn9XLyUdCD (Australiano, Masculino)

Padrão para Veo (Vertex AI + polling assíncrono):
- Requer: gcloud CLI instalado e autenticado
- Requer: GOOGLE_CLOUD_PROJECT env var (além de GOOGLE_API_KEY)
- Requer: GCS_BUCKET env var para storageUri
- Endpoint: POST https://us-central1-aiplatform.googleapis.com/v1/projects/${GOOGLE_CLOUD_PROJECT}/locations/us-central1/publishers/google/models/veo-3.1-fast-generate-001:predictLongRunning
- Polling: GET https://us-central1-aiplatform.googleapis.com/v1/{operation_name}
- Resultado em: Cloud Storage bucket especificado em parameters.storageUri

Disable-model-invocation em skills com side effects de cobrança (QUAL-01):
- elevenlabs-tts: true (gera arquivo, cobra por caractere)
- google-veo: true (gera vídeo, cobra por segundo)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar skill elevenlabs-tts (AUD-01)</name>
  <files>skills/audio/elevenlabs-tts/SKILL.md</files>

  <read_first>
    - .planning/phases/02-validation-batch/02-RESEARCH.md (seção AUD-01 e Pattern 2 — curl completo com -f e --output, lista de voice IDs estáveis)
    - skills/audio/elevenlabs-tts/ (verificar se SKILL.md já existe antes de criar)
  </read_first>

  <action>
Criar o arquivo `skills/audio/elevenlabs-tts/SKILL.md` com o conteúdo EXATO abaixo.

O skill gera áudio MP3 via ElevenLabs TTS.
USA -f (não --fail-with-body) porque a saída é binária — misturar error body com stream binário corrompe o arquivo.
Verifica [ -s "$OUTPUT_FILE" ] após o download (Pitfall 3 do RESEARCH.md).
Tem disable-model-invocation: true porque gera arquivo com cobrança por caractere (QUAL-01).
Salva em $HOME/Downloads/ com timestamp para não sobrescrever arquivos anteriores.

CONTEÚDO COMPLETO DO ARQUIVO:

```
---
name: elevenlabs-tts
description: "Text-to-speech via ElevenLabs. 32 languages, 22+ voices. Requires: ELEVENLABS_API_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate speech audio from text using the ElevenLabs API.

Ask the user for:
- TEXT: the text to convert to speech
- VOICE (optional): voice name or ID — default is "george" (see voice list below)
- OUTPUT_FILE (optional): where to save the MP3 — default: ~/Downloads/tts_[timestamp].mp3

**Available voices:**
| Name | Voice ID | Accent | Gender |
|------|----------|--------|--------|
| George (default) | JBFqnCBsd6RMkjVDRZzb | British | Male |
| Rachel | 21m00Tcm4TlvDq8ikWAM | American | Female |
| Aria | 9BWtsMINqrJLrRacOk9x | American | Female |
| Charlie | IKne3meq5aSn9XLyUdCD | Australian | Male |

Then run the following steps:

**Step 1 — Check API key:**

```bash
if [ -z "$ELEVENLABS_API_KEY" ]; then
  echo "ERROR: ELEVENLABS_API_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi
```

**Step 2 — Set voice ID:**

```bash
VOICE_INPUT="[VOICE_NAME_OR_ID_HERE]"

case "$VOICE_INPUT" in
  george|"")  VOICE_ID="JBFqnCBsd6RMkjVDRZzb" ;;
  rachel)     VOICE_ID="21m00Tcm4TlvDq8ikWAM" ;;
  aria)       VOICE_ID="9BWtsMINqrJLrRacOk9x" ;;
  charlie)    VOICE_ID="IKne3meq5aSn9XLyUdCD" ;;
  *)          VOICE_ID="$VOICE_INPUT" ;;  # Assume custom voice ID was passed directly
esac

OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/tts_$(date +%Y%m%d_%H%M%S).mp3}"
TEXT="[USER_TEXT_HERE]"
```

**Step 3 — Generate audio:**

```bash
# Use -f (not --fail-with-body) for binary output — mixing error body with binary stream corrupts the file
curl -f -s \
  -X POST "https://api.elevenlabs.io/v1/text-to-speech/${VOICE_ID}" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"$TEXT\", \"model_id\": \"eleven_multilingual_v2\", \"voice_settings\": {\"stability\": 0.5, \"similarity_boost\": 0.75}}" \
  --output "$OUTPUT_FILE"

if [ $? -ne 0 ]; then
  echo "ERROR: ElevenLabs API call failed."
  echo "Check that ELEVENLABS_API_KEY is correct in ~/.claude/.env"
  echo "Run /pocket-knife:setup to reconfigure."
  exit 1
fi

# Verify the output file has content (empty file = API error without exit code)
if [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Audio file is empty. Possible causes:"
  echo "  - Invalid or empty text input"
  echo "  - Invalid voice ID: $VOICE_ID"
  echo "  - API returned error without proper HTTP status code"
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Audio generated successfully: $OUTPUT_FILE"
```

Report to the user:
- The path where the audio file was saved
- The voice used and the text length
- If any error occurred, explain clearly (missing key -> /pocket-knife:setup, empty file -> check text/voice ID)
```
  </action>

  <verify>
    <automated>grep -q "api.elevenlabs.io/v1/text-to-speech" skills/audio/elevenlabs-tts/SKILL.md && grep -q "xi-api-key: \$ELEVENLABS_API_KEY" skills/audio/elevenlabs-tts/SKILL.md && grep -q "curl -f -s" skills/audio/elevenlabs-tts/SKILL.md && grep -q "\-\-output" skills/audio/elevenlabs-tts/SKILL.md && grep -q "\[ ! -s" skills/audio/elevenlabs-tts/SKILL.md && grep -q "disable-model-invocation: true" skills/audio/elevenlabs-tts/SKILL.md && grep -q "pocket-knife:setup" skills/audio/elevenlabs-tts/SKILL.md && echo "PASS: elevenlabs-tts skill OK" || echo "FAIL: check missing elements"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "api.elevenlabs.io/v1/text-to-speech" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
    - `grep -q "xi-api-key" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
    - `grep -q "curl -f -s" skills/audio/elevenlabs-tts/SKILL.md` retorna 0 (binário: -f, não --fail-with-body)
    - `grep -q "\-\-output" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
    - `grep -q "\[ ! -s" skills/audio/elevenlabs-tts/SKILL.md` retorna 0 (verificação de arquivo vazio)
    - `grep -q "JBFqnCBsd6RMkjVDRZzb" skills/audio/elevenlabs-tts/SKILL.md` retorna 0 (voice ID George)
    - `grep -q "eleven_multilingual_v2" skills/audio/elevenlabs-tts/SKILL.md` retorna 0 (modelo correto)
    - `grep -q "disable-model-invocation: true" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
    - `grep -q "allowed-tools: Bash(curl \*)" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
    - `grep -q "pocket-knife:setup" skills/audio/elevenlabs-tts/SKILL.md` retorna 0
  </acceptance_criteria>

  <done>SKILL.md criado com endpoint ElevenLabs correto, -f para saída binária (não --fail-with-body), verificação de arquivo vazio com [ -s ], 4 voice IDs estáveis incluídos, disable-model-invocation: true, e mensagem de erro human-readable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar skill google-veo (VID-02)</name>
  <files>skills/video/google-veo/SKILL.md</files>

  <read_first>
    - .planning/phases/02-validation-batch/02-RESEARCH.md (seção VID-02, Open Questions #1, e Pitfall 2 — endpoint Vertex AI, requisito gcloud + PROJECT_ID, padrão de polling)
    - skills/video/google-veo/ (verificar se SKILL.md já existe antes de criar)
  </read_first>

  <action>
Criar o arquivo `skills/video/google-veo/SKILL.md` com o conteúdo EXATO abaixo.

O skill usa Google Veo via Vertex AI (não generativelanguage.googleapis.com — ver Pitfall 2 do RESEARCH.md).
Requer: gcloud CLI autenticado + GOOGLE_CLOUD_PROJECT env var + GCS_BUCKET env var.
Padrão assíncrono: POST para iniciar job -> polling GET até done: true.
Tem disable-model-invocation: true (QUAL-01 — gera vídeo com cobrança por segundo).
Modelo padrão: veo-3.1-fast-generate-001 (mais rápido para validação).

CONTEÚDO COMPLETO DO ARQUIVO:

```
---
name: google-veo
description: "Generate video with Google Veo via Vertex AI. Requires: gcloud CLI + GOOGLE_CLOUD_PROJECT env var"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate video using Google Veo via the Vertex AI API. This skill uses async polling — the video generation takes 1-5 minutes.

**Requirements (beyond GOOGLE_API_KEY):**
- `gcloud` CLI installed and authenticated (`gcloud auth login`)
- `GOOGLE_CLOUD_PROJECT` set in ~/.claude/.env (your Google Cloud project ID)
- `GCS_BUCKET` set in ~/.claude/.env (a Cloud Storage bucket for output, e.g. "my-project-veo-output")
- Vertex AI API enabled in your project: https://console.cloud.google.com/apis/library/aiplatform.googleapis.com

Ask the user for:
- PROMPT: description of the video to generate
- DURATION (optional): video duration in seconds — default: 8 (max: 8 for veo-3.1-fast)
- OUTPUT_PATH (optional): GCS path suffix — default: "veo-output"

Then run the following steps:

**Step 1 — Check requirements:**

```bash
# Check gcloud CLI
if ! command -v gcloud &> /dev/null; then
  echo "ERROR: gcloud CLI not found."
  echo "Install from: https://cloud.google.com/sdk/docs/install"
  echo "Then run: gcloud auth login"
  exit 1
fi

# Check GOOGLE_CLOUD_PROJECT
if [ -z "$GOOGLE_CLOUD_PROJECT" ]; then
  echo "ERROR: GOOGLE_CLOUD_PROJECT not set."
  echo "Add GOOGLE_CLOUD_PROJECT=your-project-id to ~/.claude/.env"
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

# Check GCS_BUCKET
if [ -z "$GCS_BUCKET" ]; then
  echo "ERROR: GCS_BUCKET not set."
  echo "Add GCS_BUCKET=your-bucket-name to ~/.claude/.env"
  echo "Create a bucket at: https://console.cloud.google.com/storage"
  exit 1
fi

# Get access token
ACCESS_TOKEN=$(gcloud auth print-access-token 2>/dev/null)
if [ -z "$ACCESS_TOKEN" ]; then
  echo "ERROR: Could not get gcloud access token."
  echo "Run: gcloud auth login"
  exit 1
fi
```

**Step 2 — Start video generation job:**

```bash
PROMPT="[USER_PROMPT_HERE]"
DURATION="${DURATION:-8}"
OUTPUT_PATH="${OUTPUT_PATH:-veo-output}"
STORAGE_URI="gs://${GCS_BUCKET}/${OUTPUT_PATH}/"

RESPONSE=$(curl --fail-with-body -s \
  -X POST "https://us-central1-aiplatform.googleapis.com/v1/projects/${GOOGLE_CLOUD_PROJECT}/locations/us-central1/publishers/google/models/veo-3.1-fast-generate-001:predictLongRunning" \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"instances\": [{\"prompt\": \"$PROMPT\"}], \"parameters\": {\"storageUri\": \"$STORAGE_URI\", \"sampleCount\": 1, \"durationSeconds\": $DURATION}}" \
  2>&1)

EXIT_CODE=$?

case $EXIT_CODE in
  0) ;;
  22)
    if echo "$RESPONSE" | grep -qi "permission\|iam\|forbidden\|access denied"; then
      echo "ERROR: Permission denied. Ensure Vertex AI API is enabled and your account has 'aiplatform.endpoints.predict' permission."
      echo "Enable API: https://console.cloud.google.com/apis/library/aiplatform.googleapis.com"
    elif echo "$RESPONSE" | grep -qi "not found\|404"; then
      echo "ERROR: Project or model not found. Check GOOGLE_CLOUD_PROJECT value: $GOOGLE_CLOUD_PROJECT"
    elif echo "$RESPONSE" | grep -qi "quota\|billing"; then
      echo "ERROR: Quota exceeded or billing not enabled for project $GOOGLE_CLOUD_PROJECT"
    else
      echo "ERROR: Vertex AI API returned HTTP error."
      echo "$RESPONSE"
    fi
    exit 1
    ;;
  6)
    echo "ERROR: Could not resolve aiplatform.googleapis.com. Check internet connection."
    exit 1
    ;;
  7)
    echo "ERROR: Failed to connect to Vertex AI API. Check internet connection."
    exit 1
    ;;
  *)
    echo "ERROR: curl failed with code $EXIT_CODE"
    echo "$RESPONSE"
    exit 1
    ;;
esac

# Extract operation name
OPERATION_NAME=$(echo "$RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin).get('name', ''))" 2>/dev/null)
if [ -z "$OPERATION_NAME" ]; then
  echo "ERROR: Could not extract operation name from response."
  echo "$RESPONSE"
  exit 1
fi

echo "Video generation started. Operation: $OPERATION_NAME"
echo "Polling for completion (this may take 1-5 minutes)..."
```

**Step 3 — Poll for completion:**

```bash
MAX_ATTEMPTS=60  # 60 * 5s = 5 minutes max
ATTEMPT=0
POLL_INTERVAL=5

while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
  sleep $POLL_INTERVAL
  ATTEMPT=$((ATTEMPT + 1))
  echo "Polling attempt $ATTEMPT/$MAX_ATTEMPTS..."

  # Refresh token periodically (every 20 attempts = ~100 seconds)
  if [ $((ATTEMPT % 20)) -eq 0 ]; then
    ACCESS_TOKEN=$(gcloud auth print-access-token 2>/dev/null)
  fi

  POLL_RESPONSE=$(curl --fail-with-body -s \
    -X GET "https://us-central1-aiplatform.googleapis.com/v1/${OPERATION_NAME}" \
    -H "Authorization: Bearer $ACCESS_TOKEN" \
    2>&1)

  POLL_EXIT=$?

  if [ $POLL_EXIT -ne 0 ]; then
    echo "WARNING: Polling request failed (attempt $ATTEMPT). Retrying..."
    continue
  fi

  DONE=$(echo "$POLL_RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin).get('done', False))" 2>/dev/null)

  if [ "$DONE" = "True" ]; then
    echo "Video generation complete!"
    echo "Output saved to: $STORAGE_URI"
    echo ""
    echo "To download the video:"
    echo "  gsutil cp ${STORAGE_URI}*.mp4 ~/Downloads/"
    echo ""
    echo "Full operation response:"
    echo "$POLL_RESPONSE"
    break
  fi
done

if [ $ATTEMPT -ge $MAX_ATTEMPTS ]; then
  echo "ERROR: Video generation timed out after $((MAX_ATTEMPTS * POLL_INTERVAL)) seconds."
  echo "The operation may still be running. Check status with:"
  echo "  gcloud ai operations describe $OPERATION_NAME --region=us-central1"
  exit 1
fi
```

Report to the user:
- The GCS path where the video was saved
- How to download the video using gsutil
- The prompt and duration used
- If any error occurred, provide specific guidance (missing gcloud -> install link, permission denied -> enable Vertex AI API, quota -> billing)
```
  </action>

  <verify>
    <automated>grep -q "aiplatform.googleapis.com" skills/video/google-veo/SKILL.md && grep -q "veo-3.1-fast-generate-001" skills/video/google-veo/SKILL.md && grep -q "gcloud auth print-access-token" skills/video/google-veo/SKILL.md && grep -q "GOOGLE_CLOUD_PROJECT" skills/video/google-veo/SKILL.md && grep -q "GCS_BUCKET" skills/video/google-veo/SKILL.md && grep -q "MAX_ATTEMPTS" skills/video/google-veo/SKILL.md && grep -q "disable-model-invocation: true" skills/video/google-veo/SKILL.md && grep -q "pocket-knife:setup" skills/video/google-veo/SKILL.md && echo "PASS: google-veo skill OK" || echo "FAIL: check missing elements"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "us-central1-aiplatform.googleapis.com" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "veo-3.1-fast-generate-001:predictLongRunning" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "gcloud auth print-access-token" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "GOOGLE_CLOUD_PROJECT" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "GCS_BUCKET" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "MAX_ATTEMPTS" skills/video/google-veo/SKILL.md` retorna 0 (polling com limite)
    - `grep -q "fail-with-body" skills/video/google-veo/SKILL.md` retorna 0 (QUAL-03)
    - `grep -q "case \$EXIT_CODE" skills/video/google-veo/SKILL.md` retorna 0 (QUAL-03)
    - `grep -q "disable-model-invocation: true" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "allowed-tools: Bash(curl \*)" skills/video/google-veo/SKILL.md` retorna 0
    - `grep -q "pocket-knife:setup" skills/video/google-veo/SKILL.md` retorna 0
  </acceptance_criteria>

  <done>SKILL.md criado com endpoint Vertex AI correto para Veo, autenticação via gcloud (não GOOGLE_API_KEY), polling assíncrono com MAX_ATTEMPTS=60 (5 min timeout), verificações de pré-requisitos (gcloud, GOOGLE_CLOUD_PROJECT, GCS_BUCKET), error handling QUAL-03 com case por exit code, e disable-model-invocation: true.</done>
</task>

</tasks>

<verification>
Após todas as tasks, verificar:

```bash
# Verificar existência dos 2 arquivos
test -f skills/audio/elevenlabs-tts/SKILL.md && echo "OK: elevenlabs-tts" || echo "MISSING"
test -f skills/video/google-veo/SKILL.md && echo "OK: google-veo" || echo "MISSING"

# Verificar padrão QUAL-03 em ambos
grep -q "fail-with-body" skills/video/google-veo/SKILL.md && echo "QUAL-03 veo: OK" || echo "QUAL-03 veo: MISSING"
grep -q "curl -f -s" skills/audio/elevenlabs-tts/SKILL.md && echo "QUAL-03 tts (binary -f): OK" || echo "QUAL-03 tts: MISSING"

# Verificar disable-model-invocation em ambos (QUAL-01)
grep -q "disable-model-invocation: true" skills/audio/elevenlabs-tts/SKILL.md && echo "QUAL-01 tts: OK" || echo "QUAL-01 tts: MISSING"
grep -q "disable-model-invocation: true" skills/video/google-veo/SKILL.md && echo "QUAL-01 veo: OK" || echo "QUAL-01 veo: MISSING"

# Verificar verificação de arquivo vazio em TTS (Pitfall 3)
grep -q "\[ ! -s" skills/audio/elevenlabs-tts/SKILL.md && echo "Empty file check: OK" || echo "Empty file check: MISSING"

# Verificar polling com limite no Veo
grep -q "MAX_ATTEMPTS" skills/video/google-veo/SKILL.md && echo "Polling limit: OK" || echo "Polling limit: MISSING"

# Verificação global de todos os 5 skills da fase
grep -rq "fail-with-body\|curl -f" skills/image/ai-image-generation/SKILL.md skills/llm/llm-models/SKILL.md skills/search/web-search/SKILL.md skills/audio/elevenlabs-tts/SKILL.md skills/video/google-veo/SKILL.md 2>/dev/null && echo "QUAL-03 global: OK" || echo "QUAL-03 global: verificar arquivos do plan 01"

# Validação estrutural
bash scripts/validate-plugin.sh 2>/dev/null || echo "validate-plugin.sh não disponível ainda"
```
</verification>

<success_criteria>
- `skills/audio/elevenlabs-tts/SKILL.md` existe com `curl -f` (binário), `--output`, `[ ! -s ]`, e `disable-model-invocation: true`
- `skills/video/google-veo/SKILL.md` existe com endpoint Vertex AI, polling com MAX_ATTEMPTS, e `disable-model-invocation: true`
- Todos os 5 skills da Phase 2 (plans 01 + 02) têm curl com flag de erro (`--fail-with-body` ou `-f`)
- Skill com chave ausente exibe mensagem human-readable orientando para `/pocket-knife:setup`
- Nenhum skill faz `echo` de valores de chaves (QUAL-04)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-validation-batch/02-02-SUMMARY.md` com:
- Skills criados e seus endpoints
- Padrão de saída binária estabelecido (elevenlabs-tts)
- Padrão de polling assíncrono estabelecido (google-veo)
- Requisitos extras de Veo documentados (gcloud, GOOGLE_CLOUD_PROJECT, GCS_BUCKET)
- Qualquer desvio do plano e motivo
</output>
