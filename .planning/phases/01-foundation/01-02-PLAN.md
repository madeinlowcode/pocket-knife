---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - hooks/hooks.json
  - scripts/load-env.sh
autonomous: true
requirements:
  - FOUND-02
  - QUAL-04

must_haves:
  truths:
    - "hooks/hooks.json existe com hook SessionStart apontando para scripts/load-env.sh via ${CLAUDE_PLUGIN_ROOT}"
    - "scripts/load-env.sh existe, é executável e carrega ~/.claude/.env sem imprimir valores de chaves"
    - "load-env.sh nunca usa set -x (contrato de segurança QUAL-04)"
    - "load-env.sh nunca usa echo com variáveis de ambiente"
    - "load-env.sh usa set -o allexport / set +o allexport para exportar todas as variáveis"
  artifacts:
    - path: "hooks/hooks.json"
      provides: "Hook SessionStart que dispara o loader uma vez por sessão"
      contains: "SessionStart"
    - path: "scripts/load-env.sh"
      provides: "Loader seguro de ~/.claude/.env para o shell environment"
      contains: "SECURITY CONTRACT"
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/load-env.sh"
      via: "${CLAUDE_PLUGIN_ROOT}/scripts/load-env.sh no campo command do hook"
      pattern: "CLAUDE_PLUGIN_ROOT.*load-env"
    - from: "scripts/load-env.sh"
      to: "~/.claude/.env"
      via: "source $ENV_FILE dentro do bloco if [ -f ]"
      pattern: "source.*ENV_FILE"
---

<objective>
Criar o env loader do pocket-knife: o arquivo hooks/hooks.json que registra o hook SessionStart e o script scripts/load-env.sh que carrega ~/.claude/.env com o contrato de segurança completo (sem set -x, sem echo de valores, validação de presença com [ -z ]).

Purpose: O loader é o único mecanismo que disponibiliza API keys para todos os 85+ skills sem boilerplate por skill. Deve rodar uma vez por sessão via SessionStart hook. O contrato de segurança previne que valores de chaves vazem em logs ou outputs do Claude.

Output: hooks/hooks.json e scripts/load-env.sh — o env loader funcional com contrato de segurança documentado e auditável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Padrões confirmados pela documentação oficial do Claude Code -->

hooks/hooks.json — formato confirmado:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/load-env.sh"
          }
        ]
      }
    ]
  }
}
```

NOTAS sobre ${CLAUDE_PLUGIN_ROOT}:
- Resolve para o caminho absoluto do diretório de instalação do plugin
- Disponível em comandos de hook
- Muda quando o plugin é atualizado — correto para scripts que fazem parte do plugin

scripts/load-env.sh — implementação de referência (do RESEARCH.md):
```bash
#!/usr/bin/env bash
# SECURITY CONTRACT:
#   - Never use set -x (would print key values to stderr)
#   - Never echo or log variable values
#   - File existence is checked before sourcing

ENV_FILE="$HOME/.claude/.env"

if [ -f "$ENV_FILE" ]; then
  set -o allexport
  # shellcheck source=/dev/null
  source "$ENV_FILE"
  set +o allexport
fi
```

Contrato de segurança (QUAL-04):
- PROIBIDO: set -x — imprimiria valores de chaves no stderr capturado pelo Claude
- PROIBIDO: echo $VAR, printf "%s" "$VAR", ou qualquer impressão de valor
- PERMITIDO: [ -z "$VAR" ] para checar presença sem revelar valor
- OBRIGATÓRIO: set -o allexport / set +o allexport para exportar sem declaração explícita
- OBRIGATÓRIO: verificar se o arquivo existe antes de fazer source
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hooks/hooks.json — hook SessionStart</name>
  <files>hooks/hooks.json</files>

  <read_first>
  - .planning/phases/01-foundation/01-RESEARCH.md seção "Pattern 2: hooks.json — SessionStart Env Loader"
  - .planning/research/ARCHITECTURE.md seção "Pattern 1: SessionStart Hook as Env Loader"
  </read_first>

  <action>
Criar o arquivo `hooks/hooks.json` com o hook SessionStart que dispara o load-env.sh.

O diretório `hooks/` deve ser criado na raiz do plugin (mesmo nível que `.claude-plugin/`, `skills/`, `scripts/`).

Conteúdo exato de `hooks/hooks.json`:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/load-env.sh"
          }
        ]
      }
    ]
  }
}
```

NOTAS:
- `${CLAUDE_PLUGIN_ROOT}` é uma variável de ambiente do Claude Code — não substituir manualmente
- O hook não tem campo `matcher` (SessionStart não usa matcher; matcher é para PostToolUse)
- O hook dispara uma única vez quando a sessão inicia, antes de qualquer skill carregar
- JSON deve ser válido (aspas duplas, sem vírgula trailing)
  </action>

  <verify>
    <automated>test -f hooks/hooks.json && grep -q "SessionStart" hooks/hooks.json && grep -q "CLAUDE_PLUGIN_ROOT" hooks/hooks.json && python3 -m json.tool hooks/hooks.json > /dev/null && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
  - `test -f hooks/hooks.json` retorna exit 0
  - `grep -q '"SessionStart"' hooks/hooks.json` retorna exit 0
  - `grep -q 'CLAUDE_PLUGIN_ROOT' hooks/hooks.json` retorna exit 0
  - `grep -q 'load-env.sh' hooks/hooks.json` retorna exit 0
  - `python3 -m json.tool hooks/hooks.json` retorna exit 0 (JSON válido)
  - `grep -q '"type".*"command"' hooks/hooks.json` retorna exit 0
  - `test -d .claude-plugin/hooks` retorna exit 1 (hooks NÃO está dentro de .claude-plugin/)
  </acceptance_criteria>

  <done>hooks/hooks.json existe na raiz do plugin com hook SessionStart apontando para ${CLAUDE_PLUGIN_ROOT}/scripts/load-env.sh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/load-env.sh — loader seguro com contrato de segurança</name>
  <files>scripts/load-env.sh</files>

  <read_first>
  - scripts/validate-plugin.sh (verificações de segurança que o loader deve satisfazer — linhas com "set -x" e "SECURITY CONTRACT")
  - .planning/phases/01-foundation/01-RESEARCH.md seção "Pattern 3: load-env.sh — Loader Seguro"
  - .planning/research/ARCHITECTURE.md seção "Pattern 1: SessionStart Hook as Env Loader"
  </read_first>

  <behavior>
  - Comportamento 1: Se ~/.claude/.env existe, todas as variáveis do arquivo são exportadas para o environment do shell (usando set -o allexport + source)
  - Comportamento 2: Se ~/.claude/.env NÃO existe, o script termina silenciosamente com exit 0 (sem erro, sem mensagem)
  - Comportamento 3: Nenhum valor de variável aparece em stdout ou stderr — apenas o processo de export ocorre internamente
  - Comportamento 4: `set -x` está ausente do script (verificável com grep)
  - Comportamento 5: Nenhum `echo $VAR` ou `printf "%s" "$VAR"` com variáveis de ambiente está no script
  </behavior>

  <action>
Criar `scripts/load-env.sh` com o contrato de segurança completo.

Conteúdo exato de `scripts/load-env.sh`:
```bash
#!/usr/bin/env bash
# scripts/load-env.sh
# Pocket-Knife: loads ~/.claude/.env into the shell environment for the current session.
#
# Triggered by: hooks/hooks.json SessionStart hook
# Runs: once per Claude Code session, before any skill loads
#
# SECURITY CONTRACT:
#   - Never use `set -x` — it would print API key values to stderr (captured by Claude)
#   - Never echo, printf, or log variable values — only check presence with [ -z "$VAR" ]
#   - Always verify file existence before sourcing — graceful no-op if missing
#   - `set -o allexport` exports all variables from the file without explicit `export` per line
#
# This script intentionally has no output on success.
# Errors (e.g., syntax in .env) will appear as shell errors — acceptable and safe.

CLAUDE_ENV_FILE="$HOME/.claude/.env"

if [ -f "$CLAUDE_ENV_FILE" ]; then
  set -o allexport
  # shellcheck source=/dev/null
  source "$CLAUDE_ENV_FILE"
  set +o allexport
fi
```

Após criar o arquivo, torná-lo executável:
```bash
chmod +x scripts/load-env.sh
```

VERIFICAÇÃO DE SEGURANÇA — antes de salvar, confirmar que o arquivo:
1. NÃO contém `set -x` em nenhuma linha
2. NÃO contém `echo $` ou `printf.*\$` seguido de nome de variável de ambiente
3. CONTÉM a string `SECURITY CONTRACT` (documentação do contrato)
4. CONTÉM `set -o allexport` e `set +o allexport`
5. CONTÉM `source "$CLAUDE_ENV_FILE"` dentro de bloco `if [ -f`
  </action>

  <verify>
    <automated>
test -f scripts/load-env.sh && \
test -x scripts/load-env.sh && \
grep -q "SECURITY CONTRACT" scripts/load-env.sh && \
grep -q "set -o allexport" scripts/load-env.sh && \
grep -q 'source "\$CLAUDE_ENV_FILE"' scripts/load-env.sh && \
! grep -q "set -x" scripts/load-env.sh && \
echo "LOAD-ENV OK"
    </automated>
  </verify>

  <acceptance_criteria>
  - `test -f scripts/load-env.sh` retorna exit 0
  - `test -x scripts/load-env.sh` retorna exit 0 (arquivo executável)
  - `head -1 scripts/load-env.sh` contém `#!/usr/bin/env bash`
  - `grep -q "SECURITY CONTRACT" scripts/load-env.sh` retorna exit 0
  - `grep -q "set -o allexport" scripts/load-env.sh` retorna exit 0
  - `grep -q "set +o allexport" scripts/load-env.sh` retorna exit 0
  - `grep -q 'CLAUDE_ENV_FILE' scripts/load-env.sh` retorna exit 0
  - `! grep -q "set -x" scripts/load-env.sh` retorna exit 0 (PROIBIDO ausente)
  - `bash -n scripts/load-env.sh` retorna exit 0 (sintaxe bash válida)
  - `bash scripts/load-env.sh` retorna exit 0 mesmo sem ~/.claude/.env (graceful no-op)
  </acceptance_criteria>

  <done>scripts/load-env.sh existe, é executável, implementa o contrato de segurança QUAL-04 (sem set -x, sem echo de valores) e carrega ~/.claude/.env via set -o allexport quando o arquivo existe.</done>
</task>

</tasks>

<verification>
Após completar todas as tasks, executar:

```bash
bash scripts/validate-plugin.sh --full
```

Verificações manuais de segurança:
1. `grep -rn "set -x" scripts/` — deve retornar vazio (nenhum resultado)
2. `bash -n scripts/load-env.sh` — deve retornar exit 0 (sintaxe válida)
3. `bash scripts/load-env.sh` — deve retornar exit 0 sem output (mesmo sem ~/.claude/.env)
4. `python3 -m json.tool hooks/hooks.json` — deve retornar JSON formatado sem erro
</verification>

<success_criteria>
- `bash scripts/validate-plugin.sh --full` retorna "0 failed"
- `grep -q "SessionStart" hooks/hooks.json && grep -q "CLAUDE_PLUGIN_ROOT" hooks/hooks.json` retorna exit 0
- `grep -q "SECURITY CONTRACT" scripts/load-env.sh && ! grep -q "set -x" scripts/load-env.sh` retorna exit 0
- `test -x scripts/load-env.sh` retorna exit 0
- `bash scripts/load-env.sh` retorna exit 0 sem output
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Files created and their purpose
- Security contract verification results
- Any deviations from the plan
- Status: complete
</output>
