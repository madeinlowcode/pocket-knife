---
phase: 04-audio-video
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/audio/elevenlabs-stt/SKILL.md
  - skills/audio/elevenlabs-dialogue/SKILL.md
  - skills/audio/elevenlabs-sound-effects/SKILL.md
autonomous: true
requirements: [AUD-02, AUD-03, AUD-04, AUD-10]

must_haves:
  truths:
    - "elevenlabs-stt transcreve arquivo de audio local via multipart upload e exibe JSON com campo text"
    - "elevenlabs-dialogue gera arquivo MP3 com multiplos falantes e o salva em ~/Downloads/"
    - "elevenlabs-sound-effects gera arquivo MP3 de efeito sonoro a partir de texto e salva em ~/Downloads/"
    - "Todos os 3 skills tem disable-model-invocation: true no frontmatter (AUD-10)"
    - "Skills com ELEVENLABS_API_KEY ausente exibem mensagem orientando para /pocket-knife:setup"
  artifacts:
    - path: "skills/audio/elevenlabs-stt/SKILL.md"
      provides: "Speech-to-text via ElevenLabs Scribe v2"
      contains: "disable-model-invocation: true"
    - path: "skills/audio/elevenlabs-dialogue/SKILL.md"
      provides: "Multi-speaker dialogue audio via ElevenLabs text-to-dialogue"
      contains: "disable-model-invocation: true"
    - path: "skills/audio/elevenlabs-sound-effects/SKILL.md"
      provides: "Sound effects audio via ElevenLabs sound-generation"
      contains: "disable-model-invocation: true"
  key_links:
    - from: "elevenlabs-stt SKILL.md"
      to: "https://api.elevenlabs.io/v1/speech-to-text"
      via: "curl --fail-with-body multipart -F file=@AUDIO_FILE"
      pattern: "fail-with-body.*speech-to-text"
    - from: "elevenlabs-dialogue SKILL.md"
      to: "https://api.elevenlabs.io/v1/text-to-dialogue"
      via: "curl -f JSON body --output MP3"
      pattern: "curl -f.*text-to-dialogue"
    - from: "elevenlabs-sound-effects SKILL.md"
      to: "https://api.elevenlabs.io/v1/sound-generation"
      via: "curl -f JSON body --output MP3"
      pattern: "curl -f.*sound-generation"
---

<objective>
Criar 3 skills ElevenLabs: STT (multipart->JSON), Dialogue (JSON->binario) e Sound Effects (JSON->binario).

Purpose: Estabelecer os dois padroes base de requisicao ElevenLabs — Pattern 1 (JSON->binario) e Pattern 3 (multipart->JSON) — que serao replicados nos demais skills da Fase 4.
Output: 3 arquivos SKILL.md em skills/audio/, todos com disable-model-invocation: true (AUD-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-audio-video/04-RESEARCH.md
@skills/audio/elevenlabs-tts/SKILL.md
</context>

<interfaces>
De skills/audio/elevenlabs-tts/SKILL.md — frontmatter obrigatorio para todos os skills ElevenLabs:

  allowed-tools: Bash(curl *)
  disable-model-invocation: true

Checagem de chave padrao (reutilizar em todo skill ElevenLabs):

  if [ -z "$ELEVENLABS_API_KEY" ]; then
    echo "ERROR: ELEVENLABS_API_KEY not set."
    echo "Run /pocket-knife:setup to configure your API keys."
    exit 1
  fi

De 04-RESEARCH.md — Pattern 3: Multipart -> JSON (STT, voice-cloner):
  CRITICO: usar --fail-with-body (NAO -f) porque resposta e JSON.
  curl --fail-with-body -s -X POST "https://api.elevenlabs.io/v1/speech-to-text"
    -H "xi-api-key: $ELEVENLABS_API_KEY"
    -F "model_id=scribe_v2" -F "file=@${AUDIO_FILE}"

De 04-RESEARCH.md — Pattern 1: JSON -> Binario (sound-effects, dialogue, music):
  CRITICO: usar -f (NAO --fail-with-body) porque resposta e audio binario.
  curl -f -s -X POST "https://api.elevenlabs.io/v1/sound-generation"
    -H "xi-api-key: $ELEVENLABS_API_KEY"
    -H "Content-Type: application/json"
    -d '...' --output "$OUTPUT"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar skill elevenlabs-stt (Pattern 3: multipart -> JSON)</name>
  <files>skills/audio/elevenlabs-stt/SKILL.md</files>
  <read_first>
    - .planning/phases/04-audio-video/04-RESEARCH.md (secoes Pattern 3 e Pitfall 6)
    - skills/audio/elevenlabs-tts/SKILL.md (estrutura de referencia)
  </read_first>
  <action>
Criar o diretorio skills/audio/elevenlabs-stt/ e o arquivo SKILL.md.

CRITICO: Este skill usa --fail-with-body (NAO -f) porque a resposta e JSON.
CRITICO: Enviar APENAS file OU source_url — nao os dois (causa erro 422 na API).

Conteudo completo do SKILL.md:

---
name: elevenlabs-stt
description: "Speech-to-text via ElevenLabs Scribe v2. Transcribes audio files. Requires: ELEVENLABS_API_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Transcribe audio to text using ElevenLabs Scribe v2.

Ask the user for:
- AUDIO_FILE: absolute path to the audio file (MP3, WAV, FLAC, OGG, AAC, M4A)
- LANGUAGE (optional): language code e.g. "en", "pt", "es" — default: auto-detect
- DIARIZE (optional): identify different speakers true/false — default: false

Then run the following steps:

**Step 1 — Check API key and file:**

(bash)
if [ -z "$ELEVENLABS_API_KEY" ]; then
  echo "ERROR: ELEVENLABS_API_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

AUDIO_FILE="[ABSOLUTE_PATH_TO_AUDIO_FILE]"
LANGUAGE="${LANGUAGE:-}"
DIARIZE="${DIARIZE:-false}"

if [ ! -f "$AUDIO_FILE" ]; then
  echo "ERROR: Audio file not found: $AUDIO_FILE"
  echo "Provide an absolute path to an existing audio file (MP3, WAV, FLAC, OGG, AAC, M4A)."
  exit 1
fi
(end bash)

**Step 2 — Transcribe audio:**

(bash)
# Source: https://elevenlabs.io/docs/api-reference/speech-to-text/convert
# Use --fail-with-body (NOT -f) — response is JSON, need to see error body on failure
# Send EITHER file OR source_url — NOT both (causes 422 Unprocessable Entity)
LANG_FLAG=""
if [ -n "$LANGUAGE" ]; then
  LANG_FLAG="-F language_code=${LANGUAGE}"
fi

RESPONSE=$(curl --fail-with-body -s \
  -X POST "https://api.elevenlabs.io/v1/speech-to-text" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" \
  -F "model_id=scribe_v2" \
  -F "file=@${AUDIO_FILE}" \
  -F "diarize=${DIARIZE}" \
  $LANG_FLAG)

if [ $? -ne 0 ]; then
  echo "ERROR: ElevenLabs STT API call failed."
  echo "Response: $RESPONSE"
  echo "Check that ELEVENLABS_API_KEY is correct in ~/.claude/.env"
  echo "Run /pocket-knife:setup to reconfigure."
  exit 1
fi
(end bash)

**Step 3 — Display transcription:**

(bash)
echo "$RESPONSE" | python3 -c "
import sys, json
try:
    data = json.load(sys.stdin)
    lang = data.get('language_code', 'unknown')
    text = data.get('text', '')
    print('Language detected: ' + lang)
    print()
    print('Transcription:')
    print(text)
except json.JSONDecodeError as e:
    print('ERROR: Could not parse API response as JSON: ' + str(e))
    sys.exit(1)
"
(end bash)

Report to the user:
- The transcribed text
- The detected or specified language
- If any error occurred: missing key run /pocket-knife:setup; file not found check path; 422 error means both file and source_url were sent
  </action>
  <verify>
    <automated>grep -c "disable-model-invocation: true" skills/audio/elevenlabs-stt/SKILL.md && grep -c "fail-with-body" skills/audio/elevenlabs-stt/SKILL.md && grep -c "speech-to-text" skills/audio/elevenlabs-stt/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/audio/elevenlabs-stt/SKILL.md existe
    - Frontmatter contem: allowed-tools: Bash(curl *) e disable-model-invocation: true
    - Usa --fail-with-body (nao -f) — resposta e JSON
    - Endpoint correto: https://api.elevenlabs.io/v1/speech-to-text
    - Model: scribe_v2
    - Upload via -F "file=@${AUDIO_FILE}"
    - Validacao de arquivo local antes do curl
    - Nao envia file e source_url simultaneamente
    - Parsing de resposta JSON com python3
  </acceptance_criteria>
  <done>SKILL.md existe, tem disable-model-invocation: true, usa --fail-with-body, endpoint correto, upload multipart com scribe_v2</done>
</task>

<task type="auto">
  <name>Task 2: Criar skill elevenlabs-dialogue (Pattern 1: JSON -> binario)</name>
  <files>skills/audio/elevenlabs-dialogue/SKILL.md</files>
  <read_first>
    - .planning/phases/04-audio-video/04-RESEARCH.md (secao Pattern 1 e Code Examples ElevenLabs Dialogue)
    - skills/audio/elevenlabs-tts/SKILL.md (estrutura de referencia)
  </read_first>
  <action>
Criar o diretorio skills/audio/elevenlabs-dialogue/ e o arquivo SKILL.md.

CRITICO: Este skill usa -f (NAO --fail-with-body) porque a resposta e audio binario.
CRITICO: Modelo correto e eleven_v3 (NAO eleven_monolingual_v1 — deprecated).
CRITICO: O body JSON contem um array "inputs" com objetos {text, voice_id}.

Conteudo completo do SKILL.md:

---
name: elevenlabs-dialogue
description: "Multi-speaker dialogue audio via ElevenLabs. Up to 10 voices. Requires: ELEVENLABS_API_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate multi-speaker dialogue audio using ElevenLabs text-to-dialogue endpoint.
Each segment can use a different voice. Up to 10 unique voices per request.

Ask the user for:
- SEGMENTS: list of dialogue lines, each with text and speaker (e.g. "Alice: Hello! / Bob: Hi there!")
- OUTPUT_FILE (optional): where to save the MP3 — default: ~/Downloads/dialogue_[timestamp].mp3

**Available voices:**
| Name | Voice ID | Accent | Gender |
|------|----------|--------|--------|
| George (default male) | JBFqnCBsd6RMkjVDRZzb | British | Male |
| Rachel | 21m00Tcm4TlvDq8ikWAM | American | Female |
| Aria | 9BWtsMINqrJLrRacOk9x | American | Female |
| Sarah | EXAVITQu4vr4xnSDxMaL | American | Female |
| Charlie | IKne3meq5aSn9XLyUdCD | Australian | Male |

Then run the following steps:

**Step 1 — Check API key:**

(bash)
if [ -z "$ELEVENLABS_API_KEY" ]; then
  echo "ERROR: ELEVENLABS_API_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/dialogue_$(date +%Y%m%d_%H%M%S).mp3}"
(end bash)

**Step 2 — Generate dialogue audio:**

(bash)
# Source: https://elevenlabs.io/docs/api-reference/text-to-dialogue/convert
# Use -f (NOT --fail-with-body) for binary output — error body would corrupt the MP3 file
# Model: eleven_v3 (NOT eleven_monolingual_v1 — deprecated)
# inputs array: max 10 unique voice_ids per request
# Replace the inputs array below with the actual dialogue segments from the user
curl -f -s \
  -X POST "https://api.elevenlabs.io/v1/text-to-dialogue" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "inputs": [
      {"text": "[FIRST_LINE_TEXT]", "voice_id": "JBFqnCBsd6RMkjVDRZzb"},
      {"text": "[SECOND_LINE_TEXT]", "voice_id": "EXAVITQu4vr4xnSDxMaL"}
    ],
    "model_id": "eleven_v3"
  }' \
  --output "$OUTPUT_FILE"

if [ $? -ne 0 ]; then
  echo "ERROR: ElevenLabs Dialogue API call failed."
  echo "Check that ELEVENLABS_API_KEY is correct in ~/.claude/.env"
  echo "Run /pocket-knife:setup to reconfigure."
  exit 1
fi

# Verify the output file has content
if [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Audio file is empty. Possible causes:"
  echo "  - Invalid voice_id in inputs array"
  echo "  - API returned error without proper HTTP status code"
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Dialogue audio generated successfully: $OUTPUT_FILE"
(end bash)

Report to the user:
- The path where the audio file was saved
- Number of dialogue segments generated
- Voices used per segment
- If any error: missing key run /pocket-knife:setup; empty file check voice IDs
  </action>
  <verify>
    <automated>grep -c "disable-model-invocation: true" skills/audio/elevenlabs-dialogue/SKILL.md && grep -c "text-to-dialogue" skills/audio/elevenlabs-dialogue/SKILL.md && grep -c "eleven_v3" skills/audio/elevenlabs-dialogue/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/audio/elevenlabs-dialogue/SKILL.md existe
    - Frontmatter contem: allowed-tools: Bash(curl *) e disable-model-invocation: true
    - Usa -f (nao --fail-with-body) — resposta e audio binario
    - Endpoint correto: https://api.elevenlabs.io/v1/text-to-dialogue
    - Model: eleven_v3 (nao monolingual_v1)
    - Body JSON contem array inputs com voice_id por segmento
    - Usa --output para salvar arquivo binario
    - Verifica arquivo com [ ! -s "$OUTPUT_FILE" ] apos curl
  </acceptance_criteria>
  <done>SKILL.md existe, tem disable-model-invocation: true, usa -f, endpoint correto, modelo eleven_v3, array inputs com voice_id</done>
</task>

<task type="auto">
  <name>Task 3: Criar skill elevenlabs-sound-effects (Pattern 1: JSON -> binario)</name>
  <files>skills/audio/elevenlabs-sound-effects/SKILL.md</files>
  <read_first>
    - .planning/phases/04-audio-video/04-RESEARCH.md (secao Pattern 1 e endpoint sound-generation)
    - skills/audio/elevenlabs-tts/SKILL.md (estrutura de referencia)
  </read_first>
  <action>
Criar o diretorio skills/audio/elevenlabs-sound-effects/ e o arquivo SKILL.md.

CRITICO: Este skill usa -f (NAO --fail-with-body) porque a resposta e audio binario.
CRITICO: Endpoint e /v1/sound-generation (nao /v1/sound-effects).
CRITICO: Parametros JSON: text (obrigatorio), duration_seconds (1.0-22.0), prompt_influence (0.0-1.0).

Conteudo completo do SKILL.md:

---
name: elevenlabs-sound-effects
description: "Generate sound effects audio via ElevenLabs. Text description to MP3. Requires: ELEVENLABS_API_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate sound effects audio from a text description using ElevenLabs sound-generation API.

Ask the user for:
- TEXT: text description of the sound effect (e.g. "thunderstorm with heavy rain", "cat meowing")
- DURATION (optional): duration in seconds (1.0-22.0) — default: 5.0
- PROMPT_INFLUENCE (optional): how much the prompt influences the audio (0.0-1.0) — default: 0.3
- OUTPUT_FILE (optional): where to save the MP3 — default: ~/Downloads/sfx_[timestamp].mp3

Then run the following steps:

**Step 1 — Check API key:**

(bash)
if [ -z "$ELEVENLABS_API_KEY" ]; then
  echo "ERROR: ELEVENLABS_API_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

TEXT="[USER_DESCRIPTION_HERE]"
DURATION="${DURATION:-5.0}"
PROMPT_INFLUENCE="${PROMPT_INFLUENCE:-0.3}"
OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/sfx_$(date +%Y%m%d_%H%M%S).mp3}"
(end bash)

**Step 2 — Generate sound effect:**

(bash)
# Source: https://elevenlabs.io/docs/api-reference/text-to-sound-effects/convert
# Use -f (NOT --fail-with-body) for binary output — error body would corrupt the MP3 file
# duration_seconds: 1.0-22.0; prompt_influence: 0.0-1.0 (higher = closer to prompt)
curl -f -s \
  -X POST "https://api.elevenlabs.io/v1/sound-generation" \
  -H "xi-api-key: $ELEVENLABS_API_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"${TEXT}\", \"duration_seconds\": ${DURATION}, \"prompt_influence\": ${PROMPT_INFLUENCE}}" \
  --output "$OUTPUT_FILE"

if [ $? -ne 0 ]; then
  echo "ERROR: ElevenLabs Sound Effects API call failed."
  echo "Check that ELEVENLABS_API_KEY is correct in ~/.claude/.env"
  echo "Run /pocket-knife:setup to reconfigure."
  exit 1
fi

# Verify the output file has content (empty = API error without proper exit code)
if [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Audio file is empty. Possible causes:"
  echo "  - Text description too vague or too short"
  echo "  - duration_seconds out of range (must be 1.0-22.0)"
  echo "  - API returned error without proper HTTP status code"
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Sound effect generated successfully: $OUTPUT_FILE"
echo "Duration: ${DURATION}s | Prompt influence: ${PROMPT_INFLUENCE}"
(end bash)

Report to the user:
- The path where the audio file was saved
- The text description and parameters used
- If any error: missing key run /pocket-knife:setup; empty file check text/duration range
  </action>
  <verify>
    <automated>grep -c "disable-model-invocation: true" skills/audio/elevenlabs-sound-effects/SKILL.md && grep -c "sound-generation" skills/audio/elevenlabs-sound-effects/SKILL.md && grep -c "curl -f" skills/audio/elevenlabs-sound-effects/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/audio/elevenlabs-sound-effects/SKILL.md existe
    - Frontmatter contem: allowed-tools: Bash(curl *) e disable-model-invocation: true
    - Usa -f (nao --fail-with-body) — resposta e audio binario
    - Endpoint correto: https://api.elevenlabs.io/v1/sound-generation
    - Parametros JSON: text, duration_seconds, prompt_influence
    - Usa --output para salvar arquivo binario
    - Verifica arquivo com [ ! -s "$OUTPUT_FILE" ] apos curl
  </acceptance_criteria>
  <done>SKILL.md existe, tem disable-model-invocation: true, usa -f, endpoint /v1/sound-generation, parametros duration_seconds e prompt_influence</done>
</task>

</tasks>

<verification>
Verificacoes estaticas (executar apos concluir todas as tarefas):

1. Todos os 3 skills tem disable-model-invocation: true:
   grep -r "disable-model-invocation: true" skills/audio/elevenlabs-stt/ skills/audio/elevenlabs-dialogue/ skills/audio/elevenlabs-sound-effects/

2. STT usa --fail-with-body (nao -f):
   grep "fail-with-body" skills/audio/elevenlabs-stt/SKILL.md

3. Dialogue e Sound-Effects usam -f (nao --fail-with-body) para binario:
   grep "curl -f" skills/audio/elevenlabs-dialogue/SKILL.md skills/audio/elevenlabs-sound-effects/SKILL.md

4. Endpoints corretos:
   grep "speech-to-text" skills/audio/elevenlabs-stt/SKILL.md
   grep "text-to-dialogue" skills/audio/elevenlabs-dialogue/SKILL.md
   grep "sound-generation" skills/audio/elevenlabs-sound-effects/SKILL.md

5. Modelo nao deprecated (sem eleven_monolingual_v1):
   grep -r "eleven_monolingual_v1" skills/audio/elevenlabs-dialogue/ skills/audio/elevenlabs-sound-effects/ | wc -l
   # deve retornar 0
</verification>

<success_criteria>
- 3 SKILL.md criados nos diretorios corretos em skills/audio/
- Todos com disable-model-invocation: true (AUD-10)
- STT usa Pattern 3 (multipart + --fail-with-body): endpoint /v1/speech-to-text, model scribe_v2, -F file=@path
- Dialogue usa Pattern 1 (JSON + -f + --output): endpoint /v1/text-to-dialogue, model eleven_v3, array inputs
- Sound Effects usa Pattern 1 (JSON + -f + --output): endpoint /v1/sound-generation, parametros duration_seconds/prompt_influence
- Nenhum skill usa eleven_monolingual_v1 (deprecated)
- Error handling com mensagem human-readable orientando para /pocket-knife:setup
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/04-audio-video/04-01-SUMMARY.md com:
- Skills criados e seus endpoints
- Padroes de implementacao estabelecidos (Pattern 1 e Pattern 3)
- Decisoes de implementacao (modelos escolhidos, flags curl usadas)
- Problemas encontrados (se houver)
</output>
