---
phase: 04-audio-video
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/video/ai-video-generation/SKILL.md
  - skills/video/image-to-video/SKILL.md
  - skills/video/p-video/SKILL.md
autonomous: true
requirements: [VID-01, VID-03, VID-04]

must_haves:
  truths:
    - "ai-video-generation submete job fal.ai, exibe mensagem de progresso no polling, salva MP4 nao-vazio em ~/Downloads/"
    - "image-to-video aceita URL de imagem + prompt, submete job fal.ai, salva MP4 nao-vazio"
    - "p-video gera video via Seedance 1.0 Lite (economico), mesmo pattern de queue fal.ai"
    - "Todos os 3 skills fazem polling completo — nenhum e fire-and-forget"
    - "Polling aguarda status COMPLETED antes de fazer GET result (nao entrega resultado parcial)"
    - "Skills com FAL_KEY ausente exibem mensagem orientando para /pocket-knife:setup"
  artifacts:
    - path: "skills/video/ai-video-generation/SKILL.md"
      provides: "Text-to-video via fal.ai Kling 1.6 Standard queue"
      exports: []
    - path: "skills/video/image-to-video/SKILL.md"
      provides: "Image-to-video via fal.ai Wan 2.2 5B queue"
      exports: []
    - path: "skills/video/p-video/SKILL.md"
      provides: "Text-to-video via fal.ai Seedance 1.0 Lite (economico)"
      exports: []
  key_links:
    - from: "ai-video-generation SKILL.md"
      to: "https://queue.fal.run/fal-ai/kling-video/v1.6/standard/text-to-video"
      via: "POST submit -> GET status poll COMPLETED -> GET response -> curl download MP4"
      pattern: "queue.fal.run.*kling-video.*text-to-video"
    - from: "image-to-video SKILL.md"
      to: "https://queue.fal.run/fal-ai/wan/v2.2-5b/image-to-video"
      via: "POST com image_url -> GET status poll -> GET response -> download MP4"
      pattern: "queue.fal.run.*wan.*image-to-video"
    - from: "p-video SKILL.md"
      to: "https://queue.fal.run/fal-ai/bytedance/seedance/v1/lite/text-to-video"
      via: "POST submit -> GET status poll -> GET response -> download MP4"
      pattern: "queue.fal.run.*seedance.*text-to-video"
---

<objective>
Criar 3 skills de video usando fal.ai async queue: ai-video-generation (Kling 1.6, t2v),
image-to-video (Wan 2.2 5B, i2v) e p-video (Seedance 1.0 Lite, economico).

Purpose: Completar todos os skills de video da Phase 4 com o mesmo pattern de queue assincronas
ja estabelecido nas imagens fal.ai da Phase 3, adaptado para videos (MAX_WAIT maior, modelo diferente).
Output: 3 SKILL.md em skills/video/, todos com polling completo (nao fire-and-forget).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-audio-video/04-RESEARCH.md
@skills/video/google-veo/SKILL.md
</context>

<interfaces>
De 04-RESEARCH.md — Pattern 5: fal.ai Video Async Queue (identico ao queue de imagens Phase 3):

Endpoints (substituir MODEL pelo model ID):
  Submit:  POST  https://queue.fal.run/{MODEL}
  Status:  GET   https://queue.fal.run/{MODEL}/requests/{REQUEST_ID}/status
  Result:  GET   https://queue.fal.run/{MODEL}/requests/{REQUEST_ID}/response

Auth: Authorization: Key $FAL_KEY (nao Bearer)

Model IDs:
  ai-video-generation: fal-ai/kling-video/v1.6/standard/text-to-video
  image-to-video:      fal-ai/wan/v2.2-5b/image-to-video
  p-video:             fal-ai/bytedance/seedance/v1/lite/text-to-video

Status values: IN_QUEUE, IN_PROGRESS, COMPLETED (ou campo error presente)
CRITICO: MAX_WAIT=300 (5 minutos minimo para video); polling interval=10s

Response fields:
  request_id: extraido do submit response
  video.url:  URL do video no response final (curl download para arquivo local)

Diferenca image-to-video: adicionar "image_url" no body JSON do submit

Checagem de chave padrao:
  if [ -z "$FAL_KEY" ]; then
    echo "ERROR: FAL_KEY not set."
    echo "Run /pocket-knife:setup to configure your API keys."
    exit 1
  fi

De skills/video/google-veo/SKILL.md — padrao de polling async com MAX_ATTEMPTS e mensagem de progresso:
  while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
    sleep $POLL_INTERVAL
    ATTEMPT=$((ATTEMPT + 1))
    echo "Polling attempt $ATTEMPT/$MAX_ATTEMPTS..."
    ...verificar status...
  done
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar skill ai-video-generation (fal.ai queue text-to-video)</name>
  <files>skills/video/ai-video-generation/SKILL.md</files>
  <read_first>
    - .planning/phases/04-audio-video/04-RESEARCH.md (Pattern 5, Pitfall 4: timeout, modelos fal.ai)
    - skills/video/google-veo/SKILL.md (referencia de polling async)
  </read_first>
  <action>
Criar o diretorio skills/video/ai-video-generation/ e o arquivo SKILL.md.

CRITICO: Auth e "Authorization: Key $FAL_KEY" (nao Bearer)
CRITICO: MAX_WAIT=300 (5 minutos); polling interval=10s — videos levam 60-120s tipicamente
CRITICO: Verificar por STATUS = "COMPLETED" (maiusculas); tambem checar campo "error" na resposta
CRITICO: Download do video: extrair video.url do response JSON, entao curl -f --output
CRITICO: Modelo padrao: fal-ai/kling-video/v1.6/standard/text-to-video
CRITICO: Exibir mensagem de progresso em cada iteracao do poll

Conteudo completo do SKILL.md:

---
name: ai-video-generation
description: "Generate video from text via fal.ai Kling 1.6. Async queue. Requires: FAL_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate video from a text description using fal.ai Kling 1.6 Standard via async queue.
Video generation typically takes 1-3 minutes.

Ask the user for:
- PROMPT: detailed description of the video to generate
- DURATION (optional): video duration in seconds — default: 5 (options: 5 or 10)
- ASPECT_RATIO (optional): aspect ratio — default: "16:9" (options: "16:9", "9:16", "1:1")
- OUTPUT_FILE (optional): where to save — default: ~/Downloads/video_[timestamp].mp4

Then run the following steps:

**Step 1 — Check API key:**

(bash)
if [ -z "$FAL_KEY" ]; then
  echo "ERROR: FAL_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

PROMPT="[USER_PROMPT_HERE]"
DURATION="${DURATION:-5}"
ASPECT_RATIO="${ASPECT_RATIO:-16:9}"
OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/video_$(date +%Y%m%d_%H%M%S).mp4}"

MODEL="fal-ai/kling-video/v1.6/standard/text-to-video"
(end bash)

**Step 2 — Submit video generation job:**

(bash)
# Source: https://fal.ai/docs/model-apis/model-endpoints/queue
# Auth: "Authorization: Key $FAL_KEY" (NOT Bearer)
# Use --fail-with-body — response is JSON with request_id
echo "Submitting video generation job..."
SUBMIT=$(curl --fail-with-body -s \
  -X POST "https://queue.fal.run/${MODEL}" \
  -H "Authorization: Key $FAL_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"${PROMPT}\", \"duration\": \"${DURATION}\", \"aspect_ratio\": \"${ASPECT_RATIO}\"}")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to submit video generation job."
  echo "Response: $SUBMIT"
  echo "Check FAL_KEY in ~/.claude/.env and run /pocket-knife:setup to reconfigure."
  exit 1
fi

REQUEST_ID=$(echo "$SUBMIT" | python3 -c "import sys,json; print(json.load(sys.stdin)['request_id'])" 2>/dev/null)

if [ -z "$REQUEST_ID" ]; then
  echo "ERROR: Could not extract request_id from response."
  echo "Response: $SUBMIT"
  exit 1
fi

echo "Job submitted. request_id: $REQUEST_ID"
echo "Polling for completion (typically 1-3 minutes)..."
(end bash)

**Step 3 — Poll for completion:**

(bash)
# Status values: IN_QUEUE, IN_PROGRESS, COMPLETED (all uppercase)
# CRITICAL: check for exactly "COMPLETED"; also check for error field
# MAX_WAIT=300 (5 min) — videos take 60-120s; polling every 10s to avoid rate limiting
MAX_WAIT=300
ELAPSED=0
POLL_INTERVAL=10

while [ $ELAPSED -lt $MAX_WAIT ]; do
  sleep $POLL_INTERVAL
  ELAPSED=$((ELAPSED + POLL_INTERVAL))

  STATUS_RESP=$(curl -f -s \
    "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/status" \
    -H "Authorization: Key $FAL_KEY")

  if [ $? -ne 0 ]; then
    echo "WARNING: Status poll failed. Retrying... (${ELAPSED}/${MAX_WAIT}s)"
    continue
  fi

  STATUS=$(echo "$STATUS_RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('status',''))" 2>/dev/null)
  HAS_ERROR=$(echo "$STATUS_RESP" | python3 -c "import sys,json; d=json.load(sys.stdin); print('yes' if d.get('error') else 'no')" 2>/dev/null)

  if [ "$HAS_ERROR" = "yes" ]; then
    echo "ERROR: Video generation failed."
    echo "$STATUS_RESP"
    exit 1
  fi

  echo "Status: $STATUS — ${ELAPSED}/${MAX_WAIT}s elapsed"

  if [ "$STATUS" = "COMPLETED" ]; then
    echo "Video generation complete!"
    break
  fi
done

if [ $ELAPSED -ge $MAX_WAIT ] && [ "$STATUS" != "COMPLETED" ]; then
  echo "ERROR: Video generation timed out after ${MAX_WAIT} seconds."
  echo "request_id: $REQUEST_ID — job may still be running"
  exit 1
fi
(end bash)

**Step 4 — Fetch result and download video:**

(bash)
# Get result JSON to extract video URL
RESULT=$(curl --fail-with-body -s \
  "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/response" \
  -H "Authorization: Key $FAL_KEY")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to fetch video result."
  echo "Response: $RESULT"
  exit 1
fi

VIDEO_URL=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['video']['url'])" 2>/dev/null)

if [ -z "$VIDEO_URL" ]; then
  echo "ERROR: Could not extract video URL from result."
  echo "Result: $RESULT"
  exit 1
fi

echo "Downloading video from: $VIDEO_URL"
curl -f -s "$VIDEO_URL" --output "$OUTPUT_FILE"

if [ $? -ne 0 ] || [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Video download failed or file is empty."
  echo "Video URL was: $VIDEO_URL"
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Video saved to: $OUTPUT_FILE"
(end bash)

Report to the user:
- The path where the video was saved
- The prompt, duration, and aspect ratio used
- Total time taken
- If any error: missing FAL_KEY run /pocket-knife:setup; timeout with request_id for manual check
  </action>
  <verify>
    <automated>grep -q "disable-model-invocation: true" skills/video/ai-video-generation/SKILL.md && grep -q "kling-video" skills/video/ai-video-generation/SKILL.md && grep -q "COMPLETED" skills/video/ai-video-generation/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/video/ai-video-generation/SKILL.md existe
    - Frontmatter tem: allowed-tools: Bash(curl *) e disable-model-invocation: true
    - Auth header: "Authorization: Key $FAL_KEY" (nao Bearer)
    - Modelo: fal-ai/kling-video/v1.6/standard/text-to-video
    - 3 chamadas curl: submit (POST), poll status (GET loop), result (GET) + download
    - MAX_WAIT=300, POLL_INTERVAL=10
    - Verifica STATUS = "COMPLETED" (maiusculas) E campo error
    - Extrai video.url do result JSON e baixa com curl -f --output
    - Exibe progresso em cada iteracao do poll
  </acceptance_criteria>
  <done>SKILL.md existe, polling completo com MAX_WAIT=300, download de video binario, modelo Kling 1.6</done>
</task>

<task type="auto">
  <name>Task 2: Criar skills image-to-video e p-video</name>
  <files>skills/video/image-to-video/SKILL.md, skills/video/p-video/SKILL.md</files>
  <read_first>
    - .planning/phases/04-audio-video/04-RESEARCH.md (Pattern 5 image-to-video, modelos Wan 2.2 e Seedance 1.0 Lite)
    - skills/video/ai-video-generation/SKILL.md (recem criado — reutilizar pattern identico, so mudar MODEL e adicionar image_url)
  </read_first>
  <action>
Criar dois diretorios e dois SKILL.md. Ambos usam o mesmo Pattern 5 (fal.ai queue) de ai-video-generation.
A diferenca principal: image-to-video adiciona "image_url" no body JSON do submit.

=== ARQUIVO 1: skills/video/image-to-video/SKILL.md ===

CRITICO: Modelo: fal-ai/wan/v2.2-5b/image-to-video
CRITICO: Submit body inclui TANTO "prompt" QUANTO "image_url"
CRITICO: image_url deve ser URL publica acessivel (nao path local)
CRITICO: Auth: "Authorization: Key $FAL_KEY" (nao Bearer)
CRITICO: MAX_WAIT=300, POLL_INTERVAL=10

---
name: image-to-video
description: "Animate an image into video via fal.ai Wan 2.2. Async queue. Requires: FAL_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Animate a static image into a video using fal.ai Wan 2.2 5B image-to-video model.
The image must be a publicly accessible URL. Generation typically takes 1-3 minutes.

Ask the user for:
- IMAGE_URL: public URL of the image to animate (HTTPS URL required)
- PROMPT: description of the motion/animation to apply (e.g. "gentle waves in the background, leaves swaying")
- OUTPUT_FILE (optional): where to save — default: ~/Downloads/i2v_[timestamp].mp4

Then run the following steps:

**Step 1 — Check API key:**

(bash)
if [ -z "$FAL_KEY" ]; then
  echo "ERROR: FAL_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

IMAGE_URL="[PUBLIC_IMAGE_URL_HERE]"
PROMPT="[ANIMATION_DESCRIPTION_HERE]"
OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/i2v_$(date +%Y%m%d_%H%M%S).mp4}"

MODEL="fal-ai/wan/v2.2-5b/image-to-video"

if [ -z "$IMAGE_URL" ]; then
  echo "ERROR: IMAGE_URL is required. Provide a public HTTPS URL to the source image."
  exit 1
fi
(end bash)

**Step 2 — Submit image-to-video job:**

(bash)
# Source: https://fal.ai/models/fal-ai/wan/v2.2-5b/image-to-video/api
# Key difference from text-to-video: body includes both "prompt" AND "image_url"
# image_url must be a publicly accessible HTTPS URL
# Auth: "Authorization: Key $FAL_KEY" (NOT Bearer)
echo "Submitting image-to-video job..."
SUBMIT=$(curl --fail-with-body -s \
  -X POST "https://queue.fal.run/${MODEL}" \
  -H "Authorization: Key $FAL_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"${PROMPT}\", \"image_url\": \"${IMAGE_URL}\"}")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to submit image-to-video job."
  echo "Response: $SUBMIT"
  echo "Check FAL_KEY in ~/.claude/.env and ensure image_url is a public HTTPS URL."
  exit 1
fi

REQUEST_ID=$(echo "$SUBMIT" | python3 -c "import sys,json; print(json.load(sys.stdin)['request_id'])" 2>/dev/null)

if [ -z "$REQUEST_ID" ]; then
  echo "ERROR: Could not extract request_id from response."
  echo "Response: $SUBMIT"
  exit 1
fi

echo "Job submitted. request_id: $REQUEST_ID"
echo "Polling for completion (typically 1-3 minutes)..."
(end bash)

**Step 3 — Poll for completion:**

(bash)
MAX_WAIT=300
ELAPSED=0
POLL_INTERVAL=10

while [ $ELAPSED -lt $MAX_WAIT ]; do
  sleep $POLL_INTERVAL
  ELAPSED=$((ELAPSED + POLL_INTERVAL))

  STATUS_RESP=$(curl -f -s \
    "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/status" \
    -H "Authorization: Key $FAL_KEY")

  if [ $? -ne 0 ]; then
    echo "WARNING: Status poll failed. Retrying... (${ELAPSED}/${MAX_WAIT}s)"
    continue
  fi

  STATUS=$(echo "$STATUS_RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('status',''))" 2>/dev/null)
  HAS_ERROR=$(echo "$STATUS_RESP" | python3 -c "import sys,json; d=json.load(sys.stdin); print('yes' if d.get('error') else 'no')" 2>/dev/null)

  if [ "$HAS_ERROR" = "yes" ]; then
    echo "ERROR: Image-to-video generation failed."
    echo "$STATUS_RESP"
    exit 1
  fi

  echo "Status: $STATUS — ${ELAPSED}/${MAX_WAIT}s elapsed"

  if [ "$STATUS" = "COMPLETED" ]; then
    echo "Video generation complete!"
    break
  fi
done

if [ $ELAPSED -ge $MAX_WAIT ] && [ "$STATUS" != "COMPLETED" ]; then
  echo "ERROR: Image-to-video timed out after ${MAX_WAIT} seconds."
  echo "request_id: $REQUEST_ID"
  exit 1
fi
(end bash)

**Step 4 — Fetch result and download video:**

(bash)
RESULT=$(curl --fail-with-body -s \
  "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/response" \
  -H "Authorization: Key $FAL_KEY")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to fetch result."
  exit 1
fi

VIDEO_URL=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['video']['url'])" 2>/dev/null)

if [ -z "$VIDEO_URL" ]; then
  echo "ERROR: Could not extract video URL from result."
  echo "Result: $RESULT"
  exit 1
fi

curl -f -s "$VIDEO_URL" --output "$OUTPUT_FILE"

if [ $? -ne 0 ] || [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Video download failed or file is empty."
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Video saved to: $OUTPUT_FILE"
(end bash)

Report to the user:
- The path where the video was saved
- The source image URL and animation prompt used
- Total time taken


=== ARQUIVO 2: skills/video/p-video/SKILL.md ===

CRITICO: Modelo: fal-ai/bytedance/seedance/v1/lite/text-to-video (Seedance 1.0 Lite — economico)
CRITICO: Auth: "Authorization: Key $FAL_KEY" (nao Bearer)
CRITICO: MAX_WAIT=300, POLL_INTERVAL=10
CRITICO: Identico ao ai-video-generation, apenas modelo diferente

---
name: p-video
description: "Generate video via fal.ai Seedance 1.0 Lite. Fast and economical. Requires: FAL_KEY"
allowed-tools: Bash(curl *)
disable-model-invocation: true
---

Generate video from text using fal.ai Seedance 1.0 Lite (ByteDance). Fast and economical
alternative to Kling for quick video generation. Typically completes in 1-2 minutes.

Ask the user for:
- PROMPT: description of the video to generate
- DURATION (optional): duration in seconds — default: 5
- OUTPUT_FILE (optional): where to save — default: ~/Downloads/pvideo_[timestamp].mp4

Then run the following steps:

**Step 1 — Check API key:**

(bash)
if [ -z "$FAL_KEY" ]; then
  echo "ERROR: FAL_KEY not set."
  echo "Run /pocket-knife:setup to configure your API keys."
  exit 1
fi

PROMPT="[USER_PROMPT_HERE]"
DURATION="${DURATION:-5}"
OUTPUT_FILE="${OUTPUT_FILE:-$HOME/Downloads/pvideo_$(date +%Y%m%d_%H%M%S).mp4}"

MODEL="fal-ai/bytedance/seedance/v1/lite/text-to-video"
(end bash)

**Step 2 — Submit video generation job:**

(bash)
# Source: https://fal.ai/models/fal-ai/bytedance/seedance/v1/lite/text-to-video/api
# Seedance 1.0 Lite — fast and economical; same queue pattern as Kling
# Auth: "Authorization: Key $FAL_KEY" (NOT Bearer)
echo "Submitting video generation job (Seedance 1.0 Lite)..."
SUBMIT=$(curl --fail-with-body -s \
  -X POST "https://queue.fal.run/${MODEL}" \
  -H "Authorization: Key $FAL_KEY" \
  -H "Content-Type: application/json" \
  -d "{\"prompt\": \"${PROMPT}\", \"duration\": \"${DURATION}\"}")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to submit video generation job."
  echo "Response: $SUBMIT"
  echo "Check FAL_KEY in ~/.claude/.env and run /pocket-knife:setup to reconfigure."
  exit 1
fi

REQUEST_ID=$(echo "$SUBMIT" | python3 -c "import sys,json; print(json.load(sys.stdin)['request_id'])" 2>/dev/null)

if [ -z "$REQUEST_ID" ]; then
  echo "ERROR: Could not extract request_id."
  echo "Response: $SUBMIT"
  exit 1
fi

echo "Job submitted. request_id: $REQUEST_ID"
echo "Polling for completion (typically 1-2 minutes)..."
(end bash)

**Step 3 — Poll for completion:**

(bash)
MAX_WAIT=300
ELAPSED=0
POLL_INTERVAL=10

while [ $ELAPSED -lt $MAX_WAIT ]; do
  sleep $POLL_INTERVAL
  ELAPSED=$((ELAPSED + POLL_INTERVAL))

  STATUS_RESP=$(curl -f -s \
    "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/status" \
    -H "Authorization: Key $FAL_KEY")

  if [ $? -ne 0 ]; then
    echo "WARNING: Status poll failed. Retrying... (${ELAPSED}/${MAX_WAIT}s)"
    continue
  fi

  STATUS=$(echo "$STATUS_RESP" | python3 -c "import sys,json; print(json.load(sys.stdin).get('status',''))" 2>/dev/null)
  HAS_ERROR=$(echo "$STATUS_RESP" | python3 -c "import sys,json; d=json.load(sys.stdin); print('yes' if d.get('error') else 'no')" 2>/dev/null)

  if [ "$HAS_ERROR" = "yes" ]; then
    echo "ERROR: Video generation failed."
    echo "$STATUS_RESP"
    exit 1
  fi

  echo "Status: $STATUS — ${ELAPSED}/${MAX_WAIT}s elapsed"

  if [ "$STATUS" = "COMPLETED" ]; then
    echo "Video generation complete!"
    break
  fi
done

if [ $ELAPSED -ge $MAX_WAIT ] && [ "$STATUS" != "COMPLETED" ]; then
  echo "ERROR: Video generation timed out after ${MAX_WAIT} seconds."
  echo "request_id: $REQUEST_ID"
  exit 1
fi
(end bash)

**Step 4 — Fetch result and download video:**

(bash)
RESULT=$(curl --fail-with-body -s \
  "https://queue.fal.run/${MODEL}/requests/${REQUEST_ID}/response" \
  -H "Authorization: Key $FAL_KEY")

if [ $? -ne 0 ]; then
  echo "ERROR: Failed to fetch result."
  exit 1
fi

VIDEO_URL=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['video']['url'])" 2>/dev/null)

if [ -z "$VIDEO_URL" ]; then
  echo "ERROR: Could not extract video URL."
  echo "Result: $RESULT"
  exit 1
fi

curl -f -s "$VIDEO_URL" --output "$OUTPUT_FILE"

if [ $? -ne 0 ] || [ ! -s "$OUTPUT_FILE" ]; then
  echo "ERROR: Video download failed or file is empty."
  rm -f "$OUTPUT_FILE"
  exit 1
fi

echo "Video saved to: $OUTPUT_FILE"
(end bash)

Report to the user:
- The path where the video was saved
- The prompt and duration used
- Total time taken
- Suggest /pocket-knife:ai-video-generation for higher quality (Kling 1.6) if needed
  </action>
  <verify>
    <automated>grep -q "disable-model-invocation: true" skills/video/image-to-video/SKILL.md && grep -q "disable-model-invocation: true" skills/video/p-video/SKILL.md && grep -q "seedance" skills/video/p-video/SKILL.md && grep -q "image_url" skills/video/image-to-video/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/video/image-to-video/SKILL.md existe com disable-model-invocation: true
    - image-to-video usa modelo fal-ai/wan/v2.2-5b/image-to-video
    - image-to-video inclui "image_url" no body JSON do submit
    - image-to-video valida que IMAGE_URL nao esta vazia
    - skills/video/p-video/SKILL.md existe com disable-model-invocation: true
    - p-video usa modelo fal-ai/bytedance/seedance/v1/lite/text-to-video
    - Ambos: Auth: "Authorization: Key $FAL_KEY" (nao Bearer)
    - Ambos: MAX_WAIT=300, POLL_INTERVAL=10, STATUS = "COMPLETED"
    - Ambos: download do video via curl -f --output apos extrair video.url
  </acceptance_criteria>
  <done>2 SKILL.md criados; ambos com polling completo Pattern 5; image-to-video com image_url; p-video com Seedance Lite</done>
</task>

</tasks>

<verification>
Verificacoes estaticas apos concluir todas as tarefas:

1. Todos os 3 skills de video tem disable-model-invocation: true:
   grep -r "disable-model-invocation: true" skills/video/ai-video-generation/ skills/video/image-to-video/ skills/video/p-video/

2. Auth header correto (Key, nao Bearer):
   grep -r "Authorization: Key" skills/video/ai-video-generation/ skills/video/image-to-video/ skills/video/p-video/
   grep -r "Authorization: Bearer" skills/video/ai-video-generation/ skills/video/image-to-video/ skills/video/p-video/ | wc -l
   # segundo comando deve retornar 0

3. Model IDs corretos:
   grep "kling-video/v1.6" skills/video/ai-video-generation/SKILL.md
   grep "wan/v2.2-5b" skills/video/image-to-video/SKILL.md
   grep "seedance/v1/lite" skills/video/p-video/SKILL.md

4. Polling correto (COMPLETED em maiusculas):
   grep -r '"COMPLETED"' skills/video/ai-video-generation/ skills/video/image-to-video/ skills/video/p-video/

5. image-to-video tem image_url no submit:
   grep "image_url" skills/video/image-to-video/SKILL.md

6. Nenhum skill e fire-and-forget (todos tem loop de polling):
   grep -r "MAX_WAIT" skills/video/ai-video-generation/ skills/video/image-to-video/ skills/video/p-video/
</verification>

<success_criteria>
- 3 SKILL.md criados nos diretorios corretos em skills/video/
- Todos com disable-model-invocation: true
- ai-video-generation: modelo Kling 1.6, texto para video, Pattern 5 completo
- image-to-video: modelo Wan 2.2 5B, aceita image_url no submit, Pattern 5 completo
- p-video: modelo Seedance 1.0 Lite (economico), texto para video, Pattern 5 completo
- Todos: Auth "Key" (nao Bearer), MAX_WAIT=300, POLL_INTERVAL=10, STATUS "COMPLETED"
- Nenhum fire-and-forget: todos aguardam COMPLETED antes de fazer GET result
- Download de video: extrair video.url do JSON, curl -f --output para MP4 local
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/04-audio-video/04-03-SUMMARY.md com:
- 3 skills de video criados e seus model IDs
- Pattern de polling utilizado (MAX_WAIT, POLL_INTERVAL)
- Diferencas entre os 3 skills (text-to-video vs image-to-video, Kling vs Seedance)
- Total de skills de video no projeto (deve ser 4: google-veo + 3 novos)
</output>
